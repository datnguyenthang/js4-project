import React, { useEffect } from 'react';
import { useRoutes } from 'react-router-dom';

const Header = () => {
    return (
        <div>
            <h1>Header</h1>
        </div>
    );
};

export default Header;
