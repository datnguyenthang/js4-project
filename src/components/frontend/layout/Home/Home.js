import React, { useEffect } from 'react';
import { useRoutes } from 'react-router-dom';

const Home = () => {
    return (
        <div>
            <h1>Home page</h1>
        </div>
    );
};

export default Home;
