import React, { useEffect } from 'react';
import { useRoutes } from 'react-router-dom';

const Footer = () => {
    return (
        <div>
            <h1>Footer</h1>
        </div>
    );
};

export default Footer;
